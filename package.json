{
  "name": "gbase-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm -r build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/node": "^25.6.0",
    "dotenv": "^17.4.2",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
